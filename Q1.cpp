#include <iostream>
using namespace std;
int main()
{
    int n;
    cout << "Enter The Size Of Array: " << endl;
    cin >> n;
    int arr[n];
    cout << "Enter The Elements In array: " << endl;
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    for (int i = 0; i < n; i++)
    {
        if (arr[i] == 0)
        {
        }
    }
}